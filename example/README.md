# fojs 示例（Vue 3 + TypeScript + Vite）

这个示例项目用于验证 `fojs` 的 `.fo` 文件能力：

- 使用 TSX/JSX 语法编写 UI
- 在逻辑层直接使用 Vue 3 Composition API（`ref/computed/watch/...`），并且在 `.fo` 文件里无需手动 `import`

## 开发

建议使用 Node 20.19+（本仓库作者使用 fnm 管理 Node 版本）。

```bash
fnm use 20.19.1
npm run dev
```
